<Window x:Class="ColorLines.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ColorLines.Windows.Converters"
        Title="Color Lines"
        Width="1060"
        Height="760"
        MinWidth="820"
        MinHeight="620"
        Background="{StaticResource AppBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToHiddenVisibilityConverter x:Key="BooleanToHiddenVisibilityConverter" />
        <Style x:Key="ShellViewTransitionStyle" TargetType="Grid">
            <Setter Property="Opacity" Value="1" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="0" />
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Style.Triggers>
                <Trigger Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0.96"
                                                 To="1"
                                                 Duration="0:0:0.12"
                                                 FillBehavior="Stop" />
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                 From="6"
                                                 To="0"
                                                 Duration="0:0:0.14"
                                                 FillBehavior="Stop">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MenuPrimaryButton" TargetType="Button">
            <Setter Property="Tag" Value="MenuPrimaryButton" />
            <Setter Property="Height" Value="54" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="Background" Value="{StaticResource MenuPrimaryButtonBrush}" />
            <Setter Property="Foreground" Value="#FFFFFB" />
            <Setter Property="BorderBrush" Value="{StaticResource MenuPrimaryButtonHoverBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonFrame"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonFrame" Property="Background" Value="{StaticResource MenuPrimaryButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonFrame" Property="Background" Value="{StaticResource MenuPrimaryButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonFrame" Property="BorderBrush" Value="#FFFFE4B8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuSecondaryButton" TargetType="Button">
            <Setter Property="Tag" Value="MenuSecondaryButton" />
            <Setter Property="Height" Value="46" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Background" Value="{StaticResource MenuSecondaryButtonBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource CellBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonFrame"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonFrame" Property="Background" Value="{StaticResource MenuSecondaryButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonFrame" Property="Background" Value="{StaticResource MenuSecondaryButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonFrame" Property="BorderBrush" Value="{StaticResource MenuPrimaryButtonHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GameplayCompactButton" TargetType="Button" BasedOn="{StaticResource MenuSecondaryButton}">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Tag" Value="MenuSecondaryButton" />
        </Style>

        <Style x:Key="BoardCellButton" TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{StaticResource CellBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="#D9D0A269" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CellFrame"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CellFrame" Property="Background" Value="{StaticResource CellHoverBrush}" />
                                <Setter TargetName="CellFrame" Property="BorderBrush" Value="#D98B4A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid x:Name="MainMenuView"
              Margin="28"
              Style="{StaticResource ShellViewTransitionStyle}"
              Visibility="{Binding IsMainMenuVisible, Converter={StaticResource BooleanToHiddenVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="340" />
            </Grid.ColumnDefinitions>
            <Border x:Name="MenuBackdrop"
                    Grid.ColumnSpan="2"
                    Background="{StaticResource MenuBackdropBrush}"
                    CornerRadius="18" />

            <Grid x:Name="MenuHeroArea"
                  Grid.Column="0"
                  Margin="30,0,44,0"
                  VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Color Lines"
                               FontSize="54"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="Match five cats. Clear the board. Chase your best score."
                               Margin="0,10,0,0"
                               FontSize="18"
                               Foreground="{StaticResource TextMutedBrush}" />
                    <Border x:Name="MenuHeroBoard"
                            Margin="0,30,0,0"
                            Width="460"
                            Height="460"
                            Background="{StaticResource BoardShadowBrush}"
                            BorderBrush="{StaticResource BoardBorderBrush}"
                            BorderThickness="2"
                            CornerRadius="22"
                            Padding="18"
                            HorizontalAlignment="Left">
                        <Border Background="{StaticResource BoardInnerBrush}"
                                CornerRadius="16"
                                Padding="16">
                            <UniformGrid Rows="5" Columns="5">
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4">
                                <Image x:Name="MenuPreviewCatOrange"
                                       Source="/ColorLines.Windows;component/Assets/Themes/CozyBoard/pieces/orange.png"
                                       Width="56"
                                       Height="56"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4">
                                <Image x:Name="MenuPreviewCatBlueGray"
                                       Source="/ColorLines.Windows;component/Assets/Themes/CozyBoard/pieces/bluegray.png"
                                       Width="56"
                                       Height="56"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4">
                                <Image x:Name="MenuPreviewCatTuxedo"
                                       Source="/ColorLines.Windows;component/Assets/Themes/CozyBoard/pieces/tuxedo.png"
                                       Width="56"
                                       Height="56"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4">
                                <Image x:Name="MenuPreviewCatCalico"
                                       Source="/ColorLines.Windows;component/Assets/Themes/CozyBoard/pieces/calico.png"
                                       Width="56"
                                       Height="56"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4">
                                <Image x:Name="MenuPreviewCatWhite"
                                       Source="/ColorLines.Windows;component/Assets/Themes/CozyBoard/pieces/white.png"
                                       Width="56"
                                       Height="56"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4">
                                <Image x:Name="MenuPreviewCatGray"
                                       Source="/ColorLines.Windows;component/Assets/Themes/CozyBoard/pieces/gray.png"
                                       Width="56"
                                       Height="56"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <Border Background="{StaticResource CellBackgroundBrush}" CornerRadius="10" Margin="4" />
                            </UniformGrid>
                        </Border>
                    </Border>
                </StackPanel>
            </Grid>

            <Border x:Name="MenuCommandPanel"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="28"
                    MinWidth="320">
                <StackPanel>
                    <TextBlock Text="Ready to play"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <StackPanel x:Name="MenuStatusStrip">
                        <TextBlock Text="{Binding Game.HighScore, StringFormat=Best Score: {0}}"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock x:Name="MenuSaveSummary"
                                   Text="{Binding SaveSummaryText}"
                                   Margin="0,8,0,0"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="{Binding Game.SelectedThemeName, StringFormat=Theme: {0}}"
                                   Margin="0,8,0,0"
                                   FontSize="13"
                                   Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="{Binding Game.AnimationIntensity, StringFormat=Animation: {0}}"
                                   Margin="0,4,0,0"
                                   FontSize="13"
                                   Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="{Binding Game.IsSoundEnabled, StringFormat=Sound: {0}}"
                                   Margin="0,4,0,0"
                                   FontSize="13"
                                   Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                    <Button x:Name="ContinueButton"
                            Content="Continue"
                            Command="{Binding ContinueCommand}"
                            Style="{StaticResource MenuPrimaryButton}"
                            Margin="0,26,0,0"
                            />
                    <Button x:Name="MenuNewGameButton"
                            Content="New Game"
                            Command="{Binding NewGameCommand}"
                            Style="{StaticResource MenuSecondaryButton}"
                            Margin="0,10,0,0"
                            />
                    <Button x:Name="MenuSettingsButton"
                            Content="Settings"
                            Command="{Binding OpenSettingsCommand}"
                            Style="{StaticResource MenuSecondaryButton}"
                            Margin="0,10,0,0"
                            />
                    <Button x:Name="ExitButton"
                            Content="Exit"
                            Command="{Binding ExitCommand}"
                            Style="{StaticResource MenuSecondaryButton}"
                            Margin="0,10,0,0"
                            />
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="GameplayView"
              Margin="28"
              Style="{StaticResource ShellViewTransitionStyle}"
              Visibility="{Binding IsPlayingVisible, Converter={StaticResource BooleanToHiddenVisibilityConverter}}">
            <Grid x:Name="GameplayShell">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="320" />
                </Grid.ColumnDefinitions>

                <Grid x:Name="GameplayBoardArea"
                      Grid.Column="0"
                      Margin="0,0,24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border x:Name="MainBoardFrame"
                            Background="{StaticResource BoardShadowBrush}"
                            BorderBrush="{StaticResource BoardBorderBrush}"
                            BorderThickness="2"
                            CornerRadius="16"
                            Padding="20">
                        <Border Background="{StaticResource BoardInnerBrush}"
                                CornerRadius="12"
                                Padding="8">
                            <Viewbox Stretch="Uniform">
                                <ItemsControl ItemsSource="{Binding Game.Cells}" Width="560" Height="560">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="9" Columns="9" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource BoardCellButton}"
                                    Command="{Binding DataContext.Game.SelectCellCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    MouseEnter="BoardCellPointerEntered"
                                    MouseLeave="BoardCellPointerLeft">
                                <Grid Width="52" Height="52">
                                    <Border x:Name="ReachableTargetGlow"
                                            Margin="8"
                                            CornerRadius="16"
                                            Background="#FFFFE1A8"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsReachableTarget}" Value="True">
                                                        <Setter Property="Opacity" Value="0.42" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border x:Name="PathPreviewGlow"
                                            Margin="14"
                                            CornerRadius="14"
                                            Background="#FF7AC7C4"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPathPreview}" Value="True">
                                                        <Setter Property="Opacity" Value="0.55" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border x:Name="MovePathPulseGlow"
                                            Margin="10"
                                            CornerRadius="15"
                                            Background="#FF8FD8D1"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasMovePath}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="0.78"
                                                                                     To="0"
                                                                                     Duration="0:0:0.36" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasMovePath}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="0" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border x:Name="ClearFeedbackGlow"
                                            Background="#33FF8BA7"
                                            CornerRadius="12"
                                            Margin="3"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasCleared}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="0.62"
                                                                                     To="0"
                                                                                     Duration="0:0:0.28" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasCleared}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="0" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border x:Name="ClearPulseGlow"
                                            Background="#77FF7DA4"
                                            BorderBrush="#FFFFD0DD"
                                            BorderThickness="1"
                                            CornerRadius="12"
                                            Margin="5"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasCleared}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="0.98"
                                                                                     To="0"
                                                                                     Duration="0:0:0.42" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasCleared}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="0" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border x:Name="RejectFeedbackGlow"
                                            Background="#36D94B4B"
                                            CornerRadius="12"
                                            Margin="3"
                                            IsHitTestVisible="False">
                                        <Border.RenderTransform>
                                            <TranslateTransform />
                                        </Border.RenderTransform>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasRejectedTarget}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="0.68"
                                                                                     To="0"
                                                                                     Duration="0:0:0.28" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasRejectedTarget}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="0" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Ellipse x:Name="SpawnFeedbackGlow"
                                             Width="44"
                                             Height="30"
                                             VerticalAlignment="Bottom"
                                             Fill="#5562B96C"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasSpawned}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="0.74"
                                                                                     To="0"
                                                                                     Duration="0:0:0.34" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasSpawned}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="0" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse x:Name="MoveFeedbackGlow"
                                             Width="44"
                                             Height="30"
                                             VerticalAlignment="Bottom"
                                             Fill="#5568A9FF"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasMovedTo}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="0.74"
                                                                                     To="0"
                                                                                     Duration="0:0:0.32" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding WasMovedTo}" Value="True" />
                                                            <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="0" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse x:Name="PieceBase"
                                             Width="44"
                                             Height="23"
                                             VerticalAlignment="Bottom"
                                             Fill="#55FFF4DC"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse x:Name="PieceShadow"
                                             Width="40"
                                             Height="16"
                                             VerticalAlignment="Bottom"
                                             Fill="#3A24170F"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse Width="47"
                                             Height="34"
                                             VerticalAlignment="Bottom"
                                             Fill="#44FFE06B"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Grid x:Name="PieceActor"
                                          RenderTransformOrigin="0.5,0.85"
                                          IsHitTestVisible="False">
                                        <Grid.RenderTransform>
                                            <TranslateTransform Y="0" />
                                        </Grid.RenderTransform>
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Margin" Value="0,-3,0,3" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid x:Name="PieceScaleActor"
                                              RenderTransformOrigin="0.5,0.85">
                                            <Grid.RenderTransform>
                                                <ScaleTransform ScaleX="1" ScaleY="1" />
                                            </Grid.RenderTransform>
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding WasMovedTo}" Value="True" />
                                                                <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <MultiDataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                         From="0.35"
                                                                                         To="1"
                                                                                         Duration="0:0:0.16" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                        </MultiDataTrigger>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding WasSpawned}" Value="True" />
                                                                <Condition Binding="{Binding DataContext.Game.IsFullAnimation, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <MultiDataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                         From="0.05"
                                                                                         To="1"
                                                                                         Duration="0:0:0.18" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </MultiDataTrigger.EnterActions>
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Image x:Name="PieceImage"
                                                   Width="44"
                                                   Height="44"
                                                   Source="{Binding Piece.AssetPath}"
                                                   Stretch="Uniform"
                                                   RenderOptions.BitmapScalingMode="HighQuality">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Opacity" Value="0" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                                <Setter Property="Opacity" Value="1" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Viewbox>
                        </Border>
                    </Border>
                </Grid>

                <Border x:Name="GameplayHudPanel"
                        Grid.Column="1"
                        MaxWidth="320"
                        Background="{StaticResource GameplayHudBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="20"
                        VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border x:Name="GameplayStatusBanner"
                                Grid.Row="0"
                                MinHeight="58"
                                Background="{StaticResource GameplayHudAccentBrush}"
                                CornerRadius="10"
                                Padding="14,10">
                            <TextBlock Text="{Binding Game.StatusText}"
                                       FontSize="15"
                                       LineHeight="19"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </Border>

                        <Border x:Name="GameplayScoreBlock"
                                Grid.Row="2"
                                Background="{StaticResource PanelBackgroundBrush}"
                                BorderBrush="{StaticResource PanelBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="18,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Score"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextMutedBrush}" />
                                    <TextBlock Text="{Binding Game.Score}"
                                               FontSize="42"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                    <TextBlock Text="{Binding Game.HighScore, StringFormat=Best: {0}}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextMutedBrush}" />
                                </StackPanel>
                                <Border x:Name="ScoreDeltaBadge"
                                        Grid.Column="1"
                                        MinWidth="52"
                                        Height="40"
                                        Background="{StaticResource GameplayHudAccentBrush}"
                                        CornerRadius="10"
                                        VerticalAlignment="Bottom">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Opacity" Value="0" />
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding Game.ShowScoreDelta}" Value="True" />
                                                        <Condition Binding="{Binding Game.IsFullAnimation}" Value="True" />
                                                    </MultiDataTrigger.Conditions>
                                                    <MultiDataTrigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                 From="1"
                                                                                 To="0"
                                                                                 Duration="0:0:0.85" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </MultiDataTrigger.EnterActions>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding Game.ShowScoreDelta}" Value="True" />
                                                        <Condition Binding="{Binding Game.IsFullAnimation}" Value="False" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Opacity" Value="0" />
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Game.ScoreDeltaBadgeText}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontSize="21"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource ScoreAccentBrush}" />
                                </Border>
                            </Grid>
                        </Border>

                        <Border x:Name="GameplayNextCatsBlock"
                                Grid.Row="4"
                                Background="{StaticResource PanelBackgroundBrush}"
                                BorderBrush="{StaticResource PanelBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="18,16">
                            <StackPanel>
                                <TextBlock Text="Next Cats"
                                           FontSize="15"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextMutedBrush}" />
                                <ItemsControl ItemsSource="{Binding Game.NextPieces}" Margin="0,12,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="64"
                                                    Height="64"
                                                    Margin="0,0,10,0"
                                                    Background="{StaticResource GameplayHudAccentBrush}"
                                                    CornerRadius="14">
                                                <Image Source="{Binding AssetPath}"
                                                       Width="48"
                                                       Height="48"
                                                       Stretch="Uniform"
                                                       RenderOptions.BitmapScalingMode="HighQuality" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <StackPanel x:Name="GameplayActionBar"
                                    Grid.Row="6">
                            <Button x:Name="GameplayMenuButton"
                                    Content="Menu"
                                    Command="{Binding OpenPauseMenuCommand}"
                                    Style="{StaticResource GameplayCompactButton}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Grid x:Name="PauseMenuView"
              Background="#66000000"
              Style="{StaticResource ShellViewTransitionStyle}"
              Visibility="{Binding IsPauseMenuVisible, Converter={StaticResource BooleanToHiddenVisibilityConverter}}">
            <Border x:Name="PauseMenuPanel"
                    Width="420"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{StaticResource SettingsSurfaceBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="28">
                <StackPanel>
                    <TextBlock Text="Game Menu"
                               FontSize="34"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="The board is waiting."
                               Margin="0,8,0,22"
                               FontSize="15"
                               Foreground="{StaticResource TextMutedBrush}"
                               HorizontalAlignment="Center" />
                    <StackPanel x:Name="PauseMenuActionList">
                        <Button x:Name="PauseContinueButton"
                                Content="Continue"
                                Command="{Binding BackToGameCommand}"
                                Style="{StaticResource MenuPrimaryButton}" />
                        <Button x:Name="PauseSaveButton"
                                Content="Save Game"
                                Command="{Binding SaveGameCommand}"
                                Style="{StaticResource GameplayCompactButton}"
                                Margin="0,10,0,0" />
                        <TextBlock x:Name="PauseSaveStatusText"
                                   Text="{Binding PauseSaveStatusText}"
                                   MinHeight="18"
                                   Margin="0,6,0,0"
                                   FontSize="12"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   HorizontalAlignment="Center" />
                        <Button x:Name="PauseSettingsButton"
                                Content="Settings"
                                Command="{Binding OpenSettingsCommand}"
                                Style="{StaticResource GameplayCompactButton}"
                                Margin="0,10,0,0" />
                        <Button x:Name="PauseEndGameButton"
                                Content="End Game"
                                Command="{Binding EndGameCommand}"
                                Style="{StaticResource GameplayCompactButton}"
                                Margin="0,10,0,0" />
                        <Button x:Name="PauseBackToMenuButton"
                                Content="Return to Main Menu"
                                Command="{Binding RequestBackToMenuCommand}"
                                Style="{StaticResource GameplayCompactButton}"
                                Margin="0,10,0,0" />
                        <Border x:Name="ReturnToMenuConfirmPanel"
                                Margin="0,12,0,0"
                                Padding="14"
                                Background="{StaticResource MenuSecondaryButtonBrush}"
                                BorderBrush="{StaticResource PanelBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Visibility="{Binding IsReturnToMenuConfirmVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Return to main menu?"
                                           FontSize="15"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center" />
                                <TextBlock Text="Unsaved progress in the current run will be lost."
                                           Margin="0,6,0,12"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           TextAlignment="Center" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="ReturnToMenuCancelButton"
                                            Grid.Column="0"
                                            Content="Stay in Game"
                                            Command="{Binding CancelBackToMenuCommand}"
                                            Style="{StaticResource GameplayCompactButton}" />
                                    <Button x:Name="ReturnToMenuConfirmButton"
                                            Grid.Column="2"
                                            Content="Return Anyway"
                                            Command="{Binding ConfirmBackToMenuCommand}"
                                            Style="{StaticResource GameplayCompactButton}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="SettingsView"
              Margin="28"
              Style="{StaticResource ShellViewTransitionStyle}"
              Visibility="{Binding IsSettingsVisible, Converter={StaticResource BooleanToHiddenVisibilityConverter}}">
            <Grid x:Name="SettingsShell">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.85*" />
                    <ColumnDefinition Width="1.15*" />
                </Grid.ColumnDefinitions>

                <StackPanel x:Name="SettingsHeader"
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Margin="8,0,28,0">
                    <TextBlock Text="Settings"
                               FontSize="48"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="Tune the table before the next run."
                               Margin="0,10,0,0"
                               FontSize="18"
                               TextWrapping="Wrap"
                               Foreground="{StaticResource TextMutedBrush}" />
                    <Border Margin="0,28,0,0"
                            Background="{StaticResource GameplayHudAccentBrush}"
                            CornerRadius="12"
                            Padding="18,14"
                            MaxWidth="360"
                            HorizontalAlignment="Left">
                        <StackPanel>
                            <TextBlock Text="{Binding Game.SelectedThemeName, StringFormat=Theme: {0}}"
                                       FontSize="17"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="{Binding Game.HighScore, StringFormat=Best Score: {0}}"
                                       Margin="0,6,0,0"
                                       FontSize="14"
                                       Foreground="{StaticResource TextMutedBrush}" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Border x:Name="SettingsContentPanel"
                        Grid.Column="1"
                        MaxWidth="520"
                        Background="{StaticResource SettingsSurfaceBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="28"
                        VerticalAlignment="Center">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="24" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel x:Name="SettingsOptionList">
                            <Border x:Name="AnimationSettingRow"
                                    MinHeight="84"
                                    Background="{StaticResource SettingsRowBrush}"
                                    BorderBrush="{StaticResource PanelBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="18,14">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="160" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="Animation"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="{Binding Game.AnimationIntensity, StringFormat=Current: {0}}"
                                                   Margin="0,5,0,0"
                                                   FontSize="14"
                                                   Foreground="{StaticResource TextMutedBrush}" />
                                    </StackPanel>
                                    <Button x:Name="SettingsToggleAnimationButton"
                                            Grid.Column="1"
                                            Content="{Binding Game.AnimationToggleText}"
                                            Command="{Binding Game.ToggleAnimationCommand}"
                                            Style="{StaticResource GameplayCompactButton}"
                                            VerticalAlignment="Center" />
                                </Grid>
                            </Border>

                            <Border x:Name="SoundSettingRow"
                                    MinHeight="84"
                                    Margin="0,12,0,0"
                                    Background="{StaticResource SettingsRowBrush}"
                                    BorderBrush="{StaticResource PanelBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="18,14">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="160" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="Sound"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="{Binding Game.IsSoundEnabled, StringFormat=Enabled: {0}}"
                                                   Margin="0,5,0,0"
                                                   FontSize="14"
                                                   Foreground="{StaticResource TextMutedBrush}" />
                                    </StackPanel>
                                    <Button x:Name="SettingsToggleSoundButton"
                                            Grid.Column="1"
                                            Content="Toggle Sound"
                                            Command="{Binding Game.ToggleSoundCommand}"
                                            Style="{StaticResource GameplayCompactButton}"
                                            VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel x:Name="SettingsActionBar"
                                    Grid.Row="2">
                            <Button x:Name="SettingsNewGameButton"
                                    Content="New Game"
                                    Command="{Binding NewGameCommand}"
                                    Style="{StaticResource MenuPrimaryButton}" />
                            <Button x:Name="SettingsBackToMenuButton"
                                    Content="Back"
                                    Command="{Binding CloseSettingsCommand}"
                                    Style="{StaticResource GameplayCompactButton}"
                                    Margin="0,10,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border x:Name="GameOverOverlay"
                Background="#99000000">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsPlayingVisible}" Value="True" />
                                <Condition Binding="{Binding Game.IsGameOver}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border x:Name="GameOverDialog"
                    Width="390"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{StaticResource SettingsSurfaceBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="28">
                <StackPanel>
                    <TextBlock Text="{Binding Game.GameOverTitle}"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding Game.GameOverSummaryText}"
                               Margin="0,8,0,18"
                               FontSize="15"
                               TextWrapping="Wrap"
                               Foreground="{StaticResource TextMutedBrush}"
                               HorizontalAlignment="Center" />
                    <Border Background="{StaticResource GameplayHudAccentBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="14"
                            Margin="0,0,0,18">
                        <StackPanel>
                            <TextBlock x:Name="GameOverFinalScoreText"
                                       Text="{Binding Game.FinalScoreText}"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       HorizontalAlignment="Center" />
                            <TextBlock x:Name="GameOverBestScoreText"
                                       Text="{Binding Game.BestScoreText}"
                                       Margin="0,5,0,0"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <StackPanel x:Name="GameOverActionBar">
                        <Button x:Name="GameOverNewGameButton"
                                Content="New Game"
                                Command="{Binding NewGameCommand}"
                                Style="{StaticResource MenuPrimaryButton}" />
                        <Button x:Name="GameOverMenuButton"
                                Content="Back to Menu"
                                Command="{Binding BackToMenuCommand}"
                                Style="{StaticResource GameplayCompactButton}"
                                Margin="0,10,0,0" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>

