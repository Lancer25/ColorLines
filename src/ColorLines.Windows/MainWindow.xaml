<Window x:Class="ColorLines.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Color Lines"
        Width="1060"
        Height="760"
        MinWidth="820"
        MinHeight="620"
        Background="{StaticResource AppBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="BoardCellButton" TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{StaticResource CellBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource CellBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CellFrame"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CellFrame" Property="Background" Value="{StaticResource CellHoverBrush}" />
                                <Setter TargetName="CellFrame" Property="BorderBrush" Value="#D98B4A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource BoardBackgroundBrush}"
                BorderBrush="{StaticResource BoardBorderBrush}"
                BorderThickness="2"
                CornerRadius="16"
                Padding="20">
            <Viewbox Stretch="Uniform">
                <ItemsControl ItemsSource="{Binding Cells}" Width="560" Height="560">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="9" Columns="9" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource BoardCellButton}"
                                    Command="{Binding DataContext.SelectCellCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}">
                                <Grid Width="50" Height="50">
                                    <Border x:Name="ClearFeedbackGlow"
                                            Background="#33FF8BA7"
                                            CornerRadius="12"
                                            Margin="2"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WasCleared}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border x:Name="RejectFeedbackGlow"
                                            Background="#36D94B4B"
                                            CornerRadius="12"
                                            Margin="2"
                                            IsHitTestVisible="False">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WasRejectedTarget}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Ellipse x:Name="SpawnFeedbackGlow"
                                             Width="46"
                                             Height="36"
                                             VerticalAlignment="Bottom"
                                             Fill="#5562B96C"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WasSpawned}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse x:Name="MoveFeedbackGlow"
                                             Width="46"
                                             Height="34"
                                             VerticalAlignment="Bottom"
                                             Fill="#5568A9FF"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WasMovedTo}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse Width="42"
                                             Height="26"
                                             VerticalAlignment="Bottom"
                                             Fill="#332C1B14"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse Width="47"
                                             Height="39"
                                             VerticalAlignment="Bottom"
                                             Fill="#44FFE06B"
                                             IsHitTestVisible="False">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Polygon Points="11,18 18,4 25,20"
                                             Fill="{Binding Piece.EarBrush}"
                                             Stroke="#70452C"
                                             StrokeThickness="1">
                                        <Polygon.Style>
                                            <Style TargetType="Polygon">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Polygon.Style>
                                    </Polygon>
                                    <Polygon Points="15,16 18,9 22,18"
                                             Fill="{Binding Piece.InnerEarBrush}">
                                        <Polygon.Style>
                                            <Style TargetType="Polygon">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Polygon.Style>
                                    </Polygon>
                                    <Polygon Points="25,20 32,4 39,18"
                                             Fill="{Binding Piece.EarBrush}"
                                             Stroke="#70452C"
                                             StrokeThickness="1">
                                        <Polygon.Style>
                                            <Style TargetType="Polygon">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Polygon.Style>
                                    </Polygon>
                                    <Polygon Points="28,18 32,9 35,16"
                                             Fill="{Binding Piece.InnerEarBrush}">
                                        <Polygon.Style>
                                            <Style TargetType="Polygon">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Polygon.Style>
                                    </Polygon>
                                    <Ellipse x:Name="PieceBody"
                                             Width="42"
                                             Height="42"
                                             Margin="4,7,4,0"
                                             Fill="{Binding Piece.BodyBrush}"
                                             Stroke="#70452C"
                                             StrokeThickness="1.6">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <Ellipse Width="16"
                                             Height="8"
                                             Margin="9,14,0,0"
                                             HorizontalAlignment="Left"
                                             VerticalAlignment="Top"
                                             Fill="{Binding Piece.HighlightBrush}"
                                             Opacity="0.24" />
                                    <Grid x:Name="CatFaceLayer"
                                          Margin="0,8,0,0"
                                          IsHitTestVisible="False">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                        <Setter Property="Opacity" Value="1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Ellipse Width="4"
                                                 Height="6"
                                                 Margin="14,15,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding Piece.FaceBrush}" />
                                        <Ellipse Width="4"
                                                 Height="6"
                                                 Margin="32,15,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding Piece.FaceBrush}" />
                                        <Ellipse Width="4"
                                                 Height="3"
                                                 Margin="23,23,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding Piece.FaceBrush}" />
                                        <Path Data="M 21 27 Q 25 31 29 27"
                                              Stroke="{Binding Piece.FaceBrush}"
                                              StrokeThickness="1.4"
                                              Fill="{x:Null}"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round" />
                                        <Ellipse Width="7"
                                                 Height="4"
                                                 Margin="9,25,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="#55FFC0CB" />
                                        <Ellipse Width="7"
                                                 Height="4"
                                                 Margin="34,25,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="#55FFC0CB" />
                                    </Grid>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Viewbox>
        </Border>

        <StackPanel Grid.Column="1" Margin="28,0,0,0">
            <TextBlock Text="Color Lines"
                       FontSize="34"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="{Binding StatusText}"
                       Margin="0,6,0,26"
                       TextWrapping="Wrap"
                       FontSize="15"
                       Foreground="{StaticResource TextMutedBrush}" />

            <Border Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="#E8CFB7"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="18"
                    Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Score"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="13"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding Score}"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="34"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding HighScore, StringFormat=Best: {0}}"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="14"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding ScoreDeltaText}"
                               Foreground="{StaticResource ScoreAccentBrush}"
                               FontSize="18"
                               FontWeight="Bold" />
                </StackPanel>
            </Border>

            <Border Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="#E8CFB7"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="18"
                    Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Next Cats"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="13"
                               FontWeight="SemiBold" />
                    <ItemsControl ItemsSource="{Binding NextPieces}" Margin="0,10,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource CellHoverBrush}"
                                        CornerRadius="14"
                                        Padding="8"
                                        Margin="0,0,8,0">
                                    <Grid Width="42" Height="36">
                                        <Polygon Points="9,14 15,3 21,16"
                                                 Fill="{Binding EarBrush}"
                                                 Stroke="#70452C"
                                                 StrokeThickness="1" />
                                        <Polygon Points="13,13 15,7 18,15"
                                                 Fill="{Binding InnerEarBrush}" />
                                        <Polygon Points="21,16 27,3 33,14"
                                                 Fill="{Binding EarBrush}"
                                                 Stroke="#70452C"
                                                 StrokeThickness="1" />
                                        <Polygon Points="24,15 27,7 30,13"
                                                 Fill="{Binding InnerEarBrush}" />
                                        <Ellipse Width="34"
                                                 Height="20"
                                                 VerticalAlignment="Bottom"
                                                 Fill="#332C1B14" />
                                        <Ellipse Width="34"
                                                 Height="30"
                                                 Margin="4,8,4,0"
                                                 Fill="{Binding BodyBrush}"
                                                 Stroke="#70452C"
                                                 StrokeThickness="1" />
                                        <Ellipse Width="12"
                                                 Height="5"
                                                 Margin="10,12,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding HighlightBrush}"
                                                 Opacity="0.24" />
                                        <Ellipse Width="3"
                                                 Height="5"
                                                 Margin="13,18,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding FaceBrush}" />
                                        <Ellipse Width="3"
                                                 Height="5"
                                                 Margin="26,18,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding FaceBrush}" />
                                        <Ellipse Width="4"
                                                 Height="3"
                                                 Margin="19,25,0,0"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Top"
                                                 Fill="{Binding FaceBrush}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Button Content="New Game"
                    Command="{Binding NewGameCommand}"
                    Height="42"
                    Background="{StaticResource AccentBrush}"
                    Foreground="#FFFFFB"
                    BorderThickness="0"
                    FontSize="15"
                    FontWeight="SemiBold" />

            <Border Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="#E8CFB7"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="18"
                    Margin="0,14,0,0">
                <StackPanel>
                    <TextBlock Text="Settings"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="13"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding SelectedThemeName}"
                               Margin="0,8,0,0"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="15" />
                    <TextBlock Text="{Binding AnimationIntensity, StringFormat=Animation: {0}}"
                               Margin="0,4,0,0"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="13" />
                    <Button Content="Toggle Sound"
                            Command="{Binding ToggleSoundCommand}"
                            Height="34"
                            Margin="0,12,0,0"
                            Background="{StaticResource CellHoverBrush}"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            BorderBrush="{StaticResource CellBorderBrush}" />
                    <TextBlock Text="{Binding IsSoundEnabled, StringFormat=Sound enabled: {0}}"
                               Margin="0,6,0,0"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="12" />
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.ColumnSpan="2"
                Background="#99000000"
                Visibility="{Binding IsGameOver, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Width="320"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{StaticResource PanelBackgroundBrush}"
                    CornerRadius="14"
                    Padding="24">
                <StackPanel>
                    <TextBlock Text="Game Over"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="The board is full."
                               Margin="0,8,0,20"
                               FontSize="15"
                               Foreground="{StaticResource TextMutedBrush}"
                               HorizontalAlignment="Center" />
                    <Button Content="New Game"
                            Command="{Binding NewGameCommand}"
                            Height="42"
                            Background="{StaticResource AccentBrush}"
                            Foreground="#FFFFFB"
                            BorderThickness="0"
                            FontSize="15"
                            FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
